// Test-only stub for the "server-only" package, which otherwise throws when
// imported outside Next.js's server compilation context. Aliased in
// vitest.config.ts so unit tests can import server-only modules directly.
export {};
